version: "3"

services:
  app:
    build: .
    ports:
      - 8080:${PORT}
    volumes:
      - .:/backend
    depends_on:
      - mongo_db
      
  mongo_db:
    image: mongo:latest
    volumes:
      - ./data/db:/data/db
    restart: unless-stopped
    ports:
      - 27017:27017 